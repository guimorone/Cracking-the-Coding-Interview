// Searching in a binary tree, but the tree is not a binary search tree
// Without any property on the nodes, we might have to search through all the nodes
// O(n), where "n" is the number of nodes in the tree