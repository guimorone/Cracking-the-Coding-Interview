// product of a and b
int product(int a, int b) {
    int sum = 0;
    for(int i = 0; i < b; i++) {
        sum += a
    }

    return sum;
}

// The for loop just iterates one time through "b". So runtime = O(b)